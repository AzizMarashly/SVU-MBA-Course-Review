# STATE — PRM review (handoff file, §0d)

## 1. Settings in force
- Spec v0.11 (v0.9 at generation; re-rendered under v0.10 and v0.11), interaction mode DECIDE, pilot NONE, Arabic interface/questions/explanations, chapters 1–14 (settings table: `../PROJECT_SETTINGS.md`).
- Primary reference: `الماده الاكاديميه/MBA - Project Management - The Book.pdf` (554 pages, printed page = PDF page, 0 mismatches).
- Deliverable version: **v1.4** (consolidation pass, 2026-09-24; see §3) — before that **v1.3** (`VERSION`, scheme `MAJOR.MINOR` per `VERSIONING.md` at the repository root), 2026-09-24 (v1.0 2026-09-09, v1.1 2026-09-13, v1.2 2026-09-14). Renumbered 2026-09-24 from the retired two-digit form: v01 → v1.0, v02 → v1.1, v03 → v1.2, v04 → v1.3 (same content, no bank change); older mentions of v01…v04 below and in `qa/` file names keep the old numbers. PDF/DOCX: not produced (DECIDE default).

## 2. Stage checklist
| Stage | Status | Produced |
|---|---|---|
| 1 Read reference, verify extraction | done — PyMuPDF text; lam-alef ligature reversed by the font and fixed at character level | `extracted/book/book_fixed.txt`, `extracted/book/ch_fixed/`, `extracted/book/subsections.json` (79 level-2 subsections) |
| 2 Curriculum check, 2a chapter map | done — discriminator in `extracted/questions/others_report.md` | `chapter_map.md`, `chapter_map.json` |
| 3 Extract questions | done — 1003 raw items (book 187, soufi 162, recalls 227, others 427), 123 images/pages read visually; v04: + 34 F25 items (`f25.json`, `f25_report.md`, src34) | `extracted/questions/*.json`, `*_report.md`, `by_chapter/` |
| 4 Source ledger with hashes | done — 11 independent sources (BOOK, EX15, KIFAH, Y16, S19, F24, F25, TATI, ASEM, EMAD, MURAJA; F25 added in v04); 6 duplicate files deleted before the run | `ledger.json`, `render/SOURCES.md`, `render/meta_prm.py` (FILES, 36 rows) |
| 5 Deduplicate | done — 454 canonical in v04 (442 + 12 new from F25; 4 cross-chapter merges in `build_bank.py` MERGES); v1.4 whole-bank consolidation (I-11): **419** (8 more merges, 27 folds, log `qa/consolidation_v1.4.md`) | `render/bank_ch01..14.py` |
| 6 Conservative editing | done | — |
| 7 Verify + answer blocks, 7c low confidence | done — 69 low-confidence, 79 reconstructed (v1.4 after merges: 67, 74) | bank files, `qa/chNN_report.md` |
| 8 Cross-check | done — ASEM 20/20 agree with the book highlights; SOUFI-XCHK not a source | `qa/summary.json` |
| 9 Coverage audit + generated | done — 79/79 covered; 68 by real questions, 11 by 20 generated (21 until v03; Q12-028 became the F25 item in v04) (DECIDE default applied, no volume question asked) | `bank.json → coverage` |
| 10 Focus areas + importance | done — rule: subsections with ≥2 exam sources else top-2 with ≥1 | `bank.json → focus_table` |
| 11 Document structure, 11a–11d | done | `render/render_html.py`, `render/meta_prm.py` |
| 12 Bilingual formatting | done (RTL root, ltr spans for codes/paths) | CSS in `render_html.py` |
| 13 HTML deliverable, 13d bank checkpoint | done — v1.4 | `bank.json`, `out.html`, `../مراجعه كامله لماده ال PRM_v1.3.html`, `/S3/PRM/index.html` |
| 14 Privacy note | done (how-to + methodology) | — |
| 15 QA | done — `qa/qa_blocks_v01.txt`, `qa/browser_test_v01.md`; 20-block random sample read by eye; v03: `qa/qa_blocks_v03.txt` (§7d lists empty), Q08-007/Q10-005/Q10-022/Q09-020 checked in Chrome at desktop and 390 px; v04: `qa/qa_blocks_v04.txt` (appendix 36 rows = 36 files, §7d lists empty; not re-checked in a browser); v1.4: `qa/qa_blocks_v1.4.txt`, `qa/browser_test_v1.4.md` (desktop + 390 px), `qa/neighbours_v1.4.txt` | `qa/` |

## 3. Decisions not obvious from the files
- Audit unit = level-2 TOC headings (79); level-3 headings are topics inside them (stated in the methodology).
- Types derived from sources in `build_bank.py` (`normalise_types`): exam ⇔ exam sitting, textbook ⇔ BOOK, other ⇔ EMAD/MURAJA; ASEM adds to `sources` only.
- Exam sittings: EX15 (= PM-112هام.docx + scan + kifah p4–5, c. 2015), KIFAH (kifah p1–3, the following term), Y16, S19 (= Soufi_1 + non-book items of the Soufi bank), F24, TATI (undated pool, older vocabulary, concept-in-book items only).
- The book's chapter 2/6/8 reviews reprint chapters 1/5/7; each question is one record, placed in the chapter its content belongs to.
- Maturity-model, OPA/EEF, Delphi and network-organisation items: not in the book → out-of-scope list or low-confidence records with `book_says`.
- IMT renderer reused; `render_html.py` overrides `CHAPTERS` from the bank and reads `qa/summary.json` for methodology numbers.

- v03 (2026-09-14, prompt v0.11 §7d): the 44 table/calculation records (Q03-002; Q07-001/021/022/023; Q08-001…016; Q09-003…007/020/021; Q10-005…014/020…023/026/027) carry `table` / `ans_table` / `calc` (`T()`, `C()`, `S()` in `common.py`); shared constants per data set (`NET1/2/3`, `NET3M`, `TBL_X`, `TBL_A/B` with `_Q`, `_T`, `_ANS`, `_RES`); `why` is concept-only. An always-visible «الرموز» chip row under each such question comes from the structured `SYMBOLS` glossary in `meta_prm.py` (four fields: ar, en, f, note); tapping a chip or any underlined symbol in a table / calc cell opens a bottom sheet (touch-first, glossary embedded as JSON + `SYM_JS`); pointer devices also get a hover card. Presentation only: no answer, page, source or low-confidence flag changed; every number was recomputed by the converting helper and matched. Rules for helpers: `render/CALC_TABLE_BRIEF.md`. The build refuses a stem containing `|`; `PRM_LENIENT=1` downgrades that to a warning during partial edits.
- Q09-020/021 (levelling essays): the single `ans_table` slot holds the per-day resource histogram (the answer's deliverable); per-activity times sit in `calc.given`.
- v04 (2026-09-24): F25 (`../اسئلة سابقة/دورة F25.txt`, one student's recall, no answer key) is the 7th exam sitting. Item-by-item outcome in `extracted/questions/f25_report.md`: 12 new exam records (Q01-036, Q02-026, Q03-033/034/035, Q04-045, Q06-024, Q09-033, Q11-034/035, Q12-031/032), F25 added to 16 existing records, generated Q12-028 replaced by F25 item 24 (same fact, id kept, record sits in the generated block of `bank_ch12.py` but is type exam). A recalled item that tests the same fact from the other direction (item 2, batch production ↔ clothing) was merged into the existing record as a variant, not a new record.
- F25 items 31/32 (EVA multi-project table, CPM activity table) were recalled without numbers. Owner decision: count them as frequency evidence on the existing table-format records of the same question kind — Q10-011, Q10-012 (EVA) and Q08-007, 008, 009, 013, 014 (CPM) — each with a variant stating the F25 data differ and were not recalled. No data set was invented. **Rule settled by the owner on 2026-09-24: credit both.** A problem recalled without its numbers counts as exam-frequency evidence on the existing records of the same problem kind (students rarely remember the data, but the problem type is what matters for exam preparation). F24's number-less practical items (EVA, CPM, levelling) are therefore to be credited the same way in the next release — pending, see §5.
- F25 items 15 (FF + lag, no question) and 30 (answer "RFP" only) are not usable; listed in `f25_report.md`.
- v1.3 (2026-09-24): renumbering only (`VERSIONING.md`). `release.py` and `qa_blocks.py` accept `MAJOR.MINOR` (and still recognise the old `_vNN` names when archiving / listing); the v04 file moved to `archive/`, the course folder holds `…PRM_v1.3.html`; `bank.json` identical to v04 apart from `file_version` and the notice; cover, browser title and metadata show v1.3 next to spec v0.11.
- v1.4 (2026-09-24, IDEAS I-10/I-11, owner-approved scope "everything"): **one record per claim.** `build_bank.py` holds the pass: `MERGES` (class a, same claim and form → the dropped wording becomes a variant), `FOLDS` (same claim, other form → one main record with a «سُئل أيضاً بصيغة أخرى» block carrying form, wording, key and sources of each folded record), each entry with a one-line claim; kept record = the exam item in the chapter that owns the book page; sources, pages, raw ids unioned (frequency = size of the union); dropped ids become `aliases` (anchors). Records stay in the chapter files; never re-add a dropped record as a new id. Same sitting asked both forms → keep both with `see=[...]` (only Q01-008 ↔ Q01-009). Shared-table families are grouped on the page (`table_groups`: identical `table` in one chapter and section, plus `GROUPS_EXTRA`); Q08-014 now uses the X1…X8 names of the same data.
- v1.4 conflict: Q04-011 (TATI, key ب) and Q07-003 (S19, key د) had the same stem and options; the book settles د (p.178, scope statement before planning starts; p.177, p.244) → one record Q07-003, **low confidence kept** because p.131 supports ب. Q04-011's key in `bank_ch04.py` is now د too (the build checks it).
- v1.4 build checks (hard): same normalised option set + stem cosine ≥ 0.6 + different key (run before merging); a merge whose answers disagree where comparable; a raw source-item id on two records. Optional record field `raw` (`<file>#<index>` of `extracted/questions/*.json`, `/<label>` for one sub-question of a multi-question raw item, leading `~` for an I-10 credit that may sit on several records) — filled on the records touched in v1.4 (44/399). Warning report `qa/neighbours_v<VERSION>.txt` (cross-chapter neighbours, union rule).
- v1.4 F24 credits (settled rule above, applied to F24): P5 CPM (recalls#148) → Q08-007/008/009/013/014; P4 levelling (recalls#147) → Q09-003/004/005; P2 EVA (recalls#145) → Q10-012/013/014; P3 SPI (recalls#146) → Q10-013 only (table-format records only; the book questions Q10-020/022 were not credited). P1 FF + lead (recalls#144) stays its own low-confidence method record Q07-013 (v1.0), which predates the rule — see §5.

## 4. Known problems and open questions
- 23 answer blocks exceed 80 words without an optional line (essays/calculations/lists) — accepted under §0a, listed in `qa/qa_blocks_v01.txt`.
- "Why repeats answer" check: 30 hits inspected, all name the term legitimately; one opener («الجواب هو») fixed.
- The recalled S19/TATI EVA table has swapped column labels between sources; five records carry `low_conf` with the alternative answers.
- Not tested: printing, screen readers, real phone device. PDF/DOCX not produced.

## 5. To continue
No pending source. Follow-ups: owner decision on Q07-013 (F24 P1 FF + lead, a record built on an illustrative example; under I-10 it could instead be a credit on Q07-001, the SS + lag table record); fill `raw` on the remaining 355 records (then the raw-id check covers the whole bank); review `qa/neighbours_v1.4.txt` again after any new source; extend the §7d form to IMT; the 19 blocks over 80 words in `qa/qa_blocks_v1.4.txt` (accepted under §0a); light theme / print of the new blocks not browser-checked. For a fix: edit `render/bank_chNN.py` (for a duplicate: add a `MERGES`/`FOLDS` entry with its claim, never delete the record), bump `VERSION` (`MAJOR.MINOR`: MINOR for an edit, MAJOR only for a full regeneration; see `VERSIONING.md`), add a row to `VERSIONS.md` (with the spec version), run from `render/`:
`set PYTHONUTF8=1 && python release.py && python qa_blocks.py` (previous deliverable moves to `archive/`), then at the repository root
`python scripts/publish_page.py S3/PRM` (adds the analytics snippet) and `python scripts/build_course_index.py`, update this file.
